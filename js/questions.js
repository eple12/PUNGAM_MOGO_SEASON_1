/* =====================================================================
 * 제1회 풍암 모의고사 — 문항 데이터 로더
 *
 * 실제 문항 배열(QUESTIONS)은 여기 있지 않다. 배포용 파일에 평문으로
 * 두면 로그인 화면과 무관하게 js/questions.js 파일만 열어 봐도 문제와
 * 정답이 그대로 노출되기 때문이다.
 *
 * 진짜 데이터는 tools/questions.source.js 에 있고(절대 커밋/배포되지
 * 않음), tools/encrypt-questions.js 로 암호화해 js/questions.enc.js 를
 * 만든다. js/gate.js 가 방문자가 입력한 비밀번호로 그 파일을 복호화해
 * 이 QUESTIONS 전역을 채운 뒤에야 시험 화면이 시작된다.
 *
 * store.js 등 다른 모듈이 로드되는 시점에 이미 QUESTIONS.forEach 같은
 * 코드를 즉시 실행하므로(잠금 해제 전), 빈 배열이라도 반드시 먼저
 * 정의해 두어야 한다. 잠금 해제 후 gate.js 가 실제 배열로 덮어쓴다.
 * ===================================================================== */

var QUESTIONS = [];

/* 번호로 문항 찾기 */
function getQuestion(no) {
  return QUESTIONS[no - 1];
}
